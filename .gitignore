# Dependency directories
node_modules/
client/node_modules/

# Environment variables
.env
.env.local
# .env.production is intentionally committed — it contains only the public backend URL.
# Real secrets (DB passwords, JWT secrets) are set directly in Render's dashboard.

# Build output
dist/
.next/
build/

# Log files
*.log

# Temporary SQL files
*.pgsql # Ignore all .pgsql files

/server/uploads/